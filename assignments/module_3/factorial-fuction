#include<stdio.h>
int fecto(int no)
{
    if(no<1)
    {
        return 1;
    }
    else
    {
        return no * fecto(no-1);
    }
}
int main()
{
    int n;
    printf("enter the value of n:");
    scanf("%d",&n);
    printf("the factorial of %d is: %d",n,fecto(n));
    
}
